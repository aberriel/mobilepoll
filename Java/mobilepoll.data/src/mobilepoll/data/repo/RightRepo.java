package mobilepoll.data.repo;

import org.hibernate.Session;


public class RightRepo extends GenericRepo
{
    public RightRepo() throws Exception { super(); }
    
    public RightRepo(Session session) throws Exception { super(session); }
}